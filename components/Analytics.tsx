"use client";

/**
 * Client-side Analytics placeholder. gtag.js is loaded in <head> (app/layout.tsx).
 * Use this component to call gtag() for custom events when needed.
 */
export function Analytics() {
  return null;
}
